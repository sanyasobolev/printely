<h2>Обратная связь от клиентов</h2>
<table class ="admin">
  <tr>
    <th></th>
    <th><%= Letter.human_attribute_name("name") %></th>
    <th><%= Letter.human_attribute_name("created_at") %></th>
    <th></th>
  </tr>

    <%= render :partial => 'letter_for_admin', :collection => @letters %>

</table>
  <%= will_paginate @letters %>
  
<%= link_to 'Назад', admin_users_path, :class => 'button_style' %>
