  <tr>
    <th><%= PricelistDelivery.human_attribute_name("delivery_type") %></th>
    <th><%= PricelistDelivery.human_attribute_name("price") %></th>
    <th><%= PricelistDelivery.human_attribute_name("delivery_limit_time") %></th>
    <th><%= PricelistDelivery.human_attribute_name("territory") %></th>
    <% if current_user && current_user.has_role?('Administrator') %>
    	<th></th>
    	<th></th>
    <% end %>
  </tr>