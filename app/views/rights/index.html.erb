<h2>Права пользователей</h2>
<p><%= link_to 'Создать право', new_right_path, :class => 'box' %></p>
<table class="admin">
  <tr>
    <th></th>
    <th><%= Right.human_attribute_name("name") %></th>
    <th><%= Right.human_attribute_name("controller") %></th>
    <th><%= Right.human_attribute_name("action") %></th>
    <th><%= Right.human_attribute_name("description") %></th>
    <th></th>
  </tr>

  <%= render :partial => 'right' , :collection => @rights %>

</table>

    
    
    