  <tr class="header">
    <th><%= PricelistScan.human_attribute_name("work_desc") %></th>
    <th><%= PricelistScan.human_attribute_name("price_min") %></th>
    <th><%= PricelistScan.human_attribute_name("price_max") %></th>
    <% if current_user && current_user.has_role?('Administrator') %>
        <th><%= PricelistScan.human_attribute_name("work_name") %></th>
    	<th></th>
    	<th></th>
    <% end %>
  </tr>