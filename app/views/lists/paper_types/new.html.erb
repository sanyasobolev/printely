<h1>Создание</h1>
<%= error_messages_for :paper_type %>
<%= form_for :paper_type, :url => lists_paper_types_path do |f| %>
  <%= render :partial => 'form', :locals => {:f => f} %>
  
    <div class="submit_area">
      <%= submit_tag 'Сохранить', :class => 'submit' %>
      <%= link_to 'Назад', lists_paper_types_path, :class => 'button_style' %>
    </div>

<% end %>

 
