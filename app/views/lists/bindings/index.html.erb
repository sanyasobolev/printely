<h2><%= @title %></h2>
<p><%= link_to 'Создать', new_lists_binding_path, :class => 'button_style_long' %></p>
<table class="admin">
  <tr>
    <th></th>
    <th><%= Lists::Binding.human_attribute_name("binding") %></th>
    <th><%= Lists::Binding.human_attribute_name("price") %></th>
    <th></th>
  </tr>

  <%= render :partial => 'binding' , :collection => @bindings %>

</table>
<%= link_to 'Назад', admin_users_path, :class => 'button_style' %>
    
    
    