<h1><%= @title %></h1>
<%= error_messages_for :delivery_town %>
<%= form_for :delivery_town , :url => lists_delivery_towns_path do |f| %>
  <%= render :partial => 'form', :locals => {:f => f} %>
  
    <div class="submit_area">
      <%= submit_tag 'Сохранить', :class => 'submit' %>
      <%= link_to 'Назад', lists_delivery_towns_path, :class => 'button_style' %>
    </div>

<% end %>

 
