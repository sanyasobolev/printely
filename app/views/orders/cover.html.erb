<% if @format == 'c6' %>
<%= render :partial => 'cover_with_format', :locals => {:format => @format, :title => @title, :order => @order, :documents_quantity => @documents_quantity}  %>
<% end %>
<% if @format == 'c5' %>
<%= render :partial => 'cover_with_format', :locals => {:format => @format, :title => @title, :order => @order, :documents_quantity => @documents_quantity}  %>
<% end %>
<% if @format == 'c4' %>
<%= render :partial => 'cover_with_format', :locals => {:format => @format, :title => @title, :order => @order, :documents_quantity => @documents_quantity} %>
<% end %>




